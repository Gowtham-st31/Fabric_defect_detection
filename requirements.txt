flask>=2.3,<4
numpy>=1.23
# Server-friendly OpenCV (no GUI deps)
opencv-python-headless>=4.8
imageio-ffmpeg>=0.4
# ONNX Runtime — the only inference backend needed on Render (Linux)
onnxruntime>=1.18.0
# Production WSGI server (Linux)
gunicorn>=21
